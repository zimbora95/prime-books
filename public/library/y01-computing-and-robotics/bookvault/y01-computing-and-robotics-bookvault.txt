PRIME BOOKS -- BOOKVAULT UPLOAD SHEET
Computing & Robotics - Year 1
slug y01-computing-and-robotics
generated 2026-09-14 14:31 UTC

--------------------------------------------------------------------------
STEP 1  Add the title on bookvault.app, then enter these specs
--------------------------------------------------------------------------
  Binding ............ Perfect bound (paperback)
  Paper stock ........ Colour interior, 80 gsm uncoated
  Cover lamination ... Matte
  Trim size .......... CUSTOM 216 mm wide x 279 mm high
                       (Their own template for this book is v4_Text_279_216.pdf: 222 x 285 mm
                       with 3 mm bleed, i.e. 216 x 279 mm finished. BookVault's standard
                       list has no such size, so choose the custom option.)
  Page count ......... 180 (interior only, covers excluded)
                       = 180 pages of book. No blank pages at the rear, by request:
                       their guide suggests 191 (one less than a multiple of 12) so their
                       production barcode lands on a blank last page. Here the last page carries content.
  Colour pages ....... 180
  Mono pages ......... 0
  ISBN ............... (none yet - BookVault can issue a dummy)
  Print type ......... Standard
  Content position ... content moved 5.1 mm toward the outer edge and 0.0 mm down: closest live text is now 21.0 mm off the gutter (was 15.9), 13.0 mm from the outer trim (was 18.0) and 14.0 mm from the top trim (their rules: 20 mm gutter, 5 mm trim; tightest page 2, 15.9 mm off the gutter)

--------------------------------------------------------------------------
STEP 2  Upload the two files
--------------------------------------------------------------------------
  TEXT FILE   y01-computing-and-robotics-text-file.pdf
              180 pages, media box 222.00 x 285.00 mm = 216 x 279 mm trim + 3 mm bleed
              kept RGB: 85 Type 3 fonts block Ghostscript's CMYK conversion (guide p.6 accepts RGB, their preflight converts); transparency left on 64 pages (the flattening pass could not clear it)
  COVER FILE  y01-computing-and-robotics-cover-file.pdf
              5303 x 3366 px at 300 DPI = 448.99 x 284.99 mm
              = back 216 + spine 11 + front 216 + 3 mm bleed (guide p.18)
              spine 11 mm from 180 interior pages (padded count)
              back cover art: pdf-back

--------------------------------------------------------------------------
STEP 3  Checks
--------------------------------------------------------------------------
  [PASS] Text file page size is 222 x 285 mm
         222.00 x 285.00 mm = 216 x 279 mm trim plus 3 mm bleed (their text template is 222 x 285)
  [PASS] Bleed is 3 mm on all four edges
         guide p.5 requires 3 mm; media extends 3 mm past the trim on every side
  [PASS] Bleed continues the page edge
         96 of 96 points across the cut lines are flat
  [PASS] Page artwork survives assembly
         sampled interior pages carry the master's artwork
  [OPEN] Bleed seam matches the artwork
         a step over 10.6 mm at the bottom edge of page 91, 10.6 mm of the bleed -- a flat bleed can only approximate artwork whose colour runs along the cut as a steep gradient; the page inside the trim is unaffected
  [PASS] Live text clears their 20 mm gutter
         closest live text is 21.0 mm off the gutter (page 2); guide p.3 asks for 20 mm
  [PASS] Live text clears their 5 mm trim safety
         closest live text is 13.0 mm from the right edge (page 113); their template draws a 5 mm safety line
  [PASS] Single pages, portrait
         180 single pages, portrait, all 222.0 mm wide
  [OPEN] Page count is 12n-1
         180 pages (180 of book); guide p.5: supply one less than a divisible of 12, the production barcode is added to the last page
  [OPEN] Last page is clear for their barcode
         the final page carries content -- their barcode will be printed over it
  [PASS] Cover pages stripped from the interior
         the front cover and the back cover are removed: they belong in the cover file, and leaving them in prints the cover twice
  [PASS] All fonts embedded
         every font is embedded (guide p.3)
  [OPEN] No Type 3 glyph-program fonts
         85 Type 3 fonts. These carry headings as drawing programs instead of an embedded font, so preflight rates them lowest of all font types, and Ghostscript 10.02.1 segfaults converting them to CMYK -- this book therefore stays RGB, which BookVault accept and convert themselves
  [OPEN] Transparency flattened
         64 pages still carry it (page 7: transparency group); Ghostscript's flattening pass ran and left it in place, so it has to be cleared before a print run (guide p.3)
  [OPEN] Images at 300 DPI or better
         163 below 300 DPI, e.g. page 2: 150 DPI; page 4: 150 DPI; page 5: 150 DPI
  [PASS] Binding-edge clearance
         closest text is 23.1 mm from the left edge on page 31; guide p.3 asks for 20 mm on the gutter, their template prints 17 mm
  [PASS] No trim or bleed marks
         none are drawn, and the media box is exactly trim + bleed (222.00 x 285.00 mm), leaving no room for any
  [OPEN] Text file is PDF/X-1a:2001 with CMYK
         kept RGB: 85 Type 3 fonts block Ghostscript's CMYK conversion (guide p.6 accepts RGB, their preflight converts); transparency left on 64 pages (the flattening pass could not clear it)
  [PASS] Cover file is a perfect-bound spread
         448.99 x 284.99 mm = back 216 + spine 11 + front 216 + 3 mm bleed, by their p.18 formula
  [OPEN] Barcode area clear on the back cover
         the 38 x 25 mm box their template reserves (6.4 mm in from the right trim, 3.3 mm up) is 58.2% covered by artwork; a barcode is required printing outside the UK
  [OPEN] Spine width taken from their sizing calculator
         built at 11 mm from 180 pages x 0.056 mm per page, rounded up (guide p.18). Run home > help > sizing calculator for the real paper and rebuild with --spine-mm <mm>

--------------------------------------------------------------------------
NOTES
--------------------------------------------------------------------------
  * In a Prime Books PDF, page 1 is the front cover and the last page
    is the back cover. Both are reprinted inside the cover file, so
    they are removed from the text file: y01-computing-and-robotics.pdf has 182 pages,
    this text file has 180.
  * Rules followed here are from BookVault's 'Your Guide to Supplying
    Print-Ready PDF Files', kept in docs/bookvault/ together with the
    two templates they published for this book:
      bleed 3 mm on every edge (p.5); text file media box is
      trim + bleed = 222 x 285 mm (p.5,p.18)
      page count one less than a multiple of 12: 180 (p.5)
      first page prints on the first right-hand page (p.5), so page 1 is a
      recto. Prime Books pages are not mirrored, so no layout moves; the
      binding-edge check below measures the real clearance instead
      covers inset: content kept 5 mm clear of the cover trim (p.19)
  * Bleed is extended from each page's own edge artwork, so the running-
    header band and any full-bleed art carry to the trim line with no gap.
  * Spine: guide p.18 takes it from the sizing calculator and rounds UP to
    a whole mm. This build uses 0.056 mm per page (their 80gsm bond example:
    100 pages = 5.6 mm). Run the calculator for the paper you choose, then
    rebuild with --spine-mm <mm> and the OPEN check below clears.
  * Colour: guide p.6 prefers CMYK with a FOGRA profile, and accepts RGB.
    This file is converted to CMYK through Ghostscript's default CMYK
    profile, not a FOGRA one - ask BookVault for their output profile and
    rebuild with --profile <icc> if the press is calibrated.
  * This book carries 85 Type 3 fonts (headings drawn as
    glyph programs rather than an embedded font). Ghostscript 10.02.1
    segfaults converting those to CMYK, so the interior is RGB - which
    BookVault accept and convert themselves (guide p.6). The real fix
    is in the source book: those headings should use an embedded font.
  * 64 pages still carry transparency that
    Ghostscript's flattening pass could not clear; run a preflight
    flatten before the print run, or fix the same source.

